## spree-store umbrella — tenant-neutral defaults.
## Per-tenant overlays live in customers/<slug>/base/values.yaml.
## Per-env overlays live in customers/<slug>/{stg,prd}/values.yaml.

## @section Top-level
## env + customer.slug come from the per-tenant overlays; namespace is derived.
env: ""                  # "stg" | "prd" — env overlay
namespace: ""            # auto: "{customer.slug}-{env}" — see _helpers.tpl

customer:
  slug: ""               # tenant base overlay

clusterDomain: cluster.local

## @section Hostnames + gateway wiring
## All four hostnames come from the per-env overlay.
## `storefront` may be a single host (string) or a list of hosts for a
## multi-domain store (one market per domain), e.g.
##   storefront:
##     - example.com
##     - example.us
##     - example.uk
##     - example.eu
hostnames:
  admin: ""
  api: ""
  storefront: ""
  cms: ""

publicURL:
  admin: ""
  api: ""
  storefront: ""
  cms: ""

gateway:
  name: main
  namespace: gateway
  sectionNames:
    fitness: []          # fallback for admin + api when .admin/.api unset
    com: []              # cms route (+ storefront fallback if .storefront unset)
    # admin / api: per-route listeners. When the admin host is on the apex and
    # the api host on a subdomain (or vice versa) they need DIFFERENT listeners
    # — set each so its single hostname intersects exactly one listener (else a
    # parentRef reports NoMatchingListenerHostname). Both fall back to .fitness.
    #   admin: [example-fitness-https]            # apex listener
    #   api:   [wildcard-example-fitness-https]   # wildcard listener
    admin: []
    api: []
    # storefront: listeners the storefront domains attach to. When unset the
    # storefront route falls back to `.com`. For a multi-domain store, list
    # every base-domain listener, e.g. (prod apex listeners):
    #   storefront:
    #     - example-com-https
    #     - example-us-https
    #     - example-uk-https
    #     - example-eu-https
    storefront: []

## @section CNPG cluster (shared by spree + payload)
cnpg:
  name: ""               # tenant base overlay — typically "<slug>-db"
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:18
  storageSize: 20Gi
  walStorageSize: 5Gi
  storageClass: ceph-block-r1
  walStorageClass: ""
  maxSyncReplicas: 0

  # CNPG pod resources. Default sized for throwaway envs (stg); prd overlays
  # bump to production sizing to match the legacy tkf pg-primary cluster.
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 1Gi

  # postgresql.parameters passed through to the Cluster CR. Empty by default
  # (CNPG defaults apply); prd overlays set production tuning.
  parameters: {}

  # CNPG spec.affinity (AffinityConfiguration). Empty by default → CNPG's
  # built-in soft (preferred) pod anti-affinity applies, fine for throwaway
  # stg. prd overlays set required hostname anti-affinity for one-pod-per-node.
  affinity: {}

  pooler:
    enabled: true
    instances: 1
    maxClientConn: "200"
    defaultPoolSize: "10"

  scheduledBackup:
    enabled: false
    schedule: "0 0 * * * *"
    # Take a base backup as soon as the ScheduledBackup is created (one-shot
    # on create, then on schedule). prd sets true so the cluster is
    # restorable immediately rather than waiting for the first cron tick.
    immediate: false

  objectStore:
    enabled: false
    bucket: ""
    path: ""
    retentionPolicy: "30d"
    retentionPolicyIntervalSeconds: 1800
    walCompression: gzip

  ## Initial databases + owners. Each gets a Database CR (CNPG ≥1.24) so we
  ## don't depend on postInitSQL (which doesn't run during recovery-from-backup).
  databases: []          # tenant base overlay — see customers/tkf/base/values.yaml

## @section Dragonfly engine image (shared by all redis-* instances below)
## Every redis-{queue,cache,cable} instance runs the same Dragonfly build; the
## image lives here once and the three instance blocks reference it.
dragonfly:
  image:
    # docker.dragonflydb.io IS the registry; previous values doubled it up
    # under docker.io which resolved to a non-existent docker.io image.
    registry: docker.dragonflydb.io
    repository: dragonflydb/dragonfly
    # Pinned for reproducibility — `latest` lets a pod reschedule silently pull a
    # new Dragonfly major. v1.39.0 is the build prd has been running (and clears
    # the v1.13+ floor for --shard_round_robin_prefix). Bump deliberately.
    tag: v1.39.0

## @section Redis queue Dragonfly (Sidekiq job queues)
## Persistent store for Sidekiq queues (REDIS_URL=…/0 on web + worker). Runs
## WITHOUT eviction (Dragonfly OOMs when full) because losing a queued/in-flight
## job is data loss. Do NOT point eviction-tolerant cache load here — that's
## `redisCache`. ActionCable moved off this to `redisCable` in 0.5.0.
## (Renamed from `dragonfly` in 0.5.0; resource name `{release}-redis-queue`.)
redisQueue:
  enabled: true
  replicaCount: 1
  proactorThreads: 2
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      memory: 1Gi

## @section Redis cable Dragonfly (ActionCable pub/sub)
## Dedicated instance for ActionCable pub/sub (CABLE_REDIS_URL=…/0). Pub/sub is
## transient (no stored keys), so it stays tiny and runs without eviction or
## persistence. Requires a backend image whose config/cable.yml reads
## CABLE_REDIS_URL (falls back to REDIS_URL on older images). New in 0.5.0.
## NOTE: Dragonfly refuses to boot unless it sees >= 256MiB available PER
## proactor thread, so the memory LIMIT (which it reads as available memory)
## must clear 256Mi×proactorThreads with headroom — hence 512Mi for 1 thread.
## A 256Mi limit crash-loops with "256.00MiB are required. Exiting".
redisCable:
  enabled: true
  replicaCount: 1
  proactorThreads: 1
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 512Mi

## @section Redis cache Dragonfly (shared LRU cache backend)
## A DEDICATED eviction-tolerant Redis-compatible instance shared by:
##   * the Next.js storefront cache handlers (ISR + Cache Components) — db 0
##   * the Rails backend `Rails.cache` (REDIS_CACHE_URL) — db 1
## Kept separate from the redisQueue instance above so page + fragment cache
## can never evict or OOM job state. Runs with `--cache_mode=true` + `--maxmemory`
## so under pressure it evicts least-recently-used entries (all carry TTLs)
## instead of failing — exactly what a cache wants. Ephemeral; cold-start
## repopulates, so no persistence.
## (Renamed from `storefrontCache` in 0.4.0 — now serves the backend too.)
redisCache:
  enabled: true
  replicaCount: 1
  # Two proactor threads now that both the storefront and Rails share it.
  proactorThreads: 2
  # Image reused from .Values.dragonfly.image (same Dragonfly build).
  # Eviction ceiling — held at ~75% of the container memory limit so Dragonfly's
  # own per-shard bookkeeping has headroom before the cgroup OOM-kills it.
  maxmemory: 576mb
  resources:
    requests:
      cpu: 100m
      memory: 384Mi
    limits:
      memory: 768Mi

## @section Meilisearch (product search engine)
## Optional, opinion-free, disabled by default so tenants on the DB search
## provider are unaffected. When enabled, Spree's built-in
## Spree::SearchProvider::Meilisearch takes over IFF the backend also gets
## MEILISEARCH_URL set (wired in the consuming overlay's _railsEnv anchor).
## Single-node by design — Meilisearch OSS has no clustering, so durability
## comes from a PVC (index survives restarts) + Spree's auto-index-on-save,
## NOT from replicas. The PVC is RWO, so the Deployment uses strategy Recreate.
## Auth: MEILI_MASTER_KEY is read from the spree-rails Secret (key
## MEILISEARCH_API_KEY) — see templates/externalsecrets.yaml. Pin the image tag
## (an on-disk index format lives in the PVC; do NOT track `latest`).
meilisearch:
  enabled: false
  image:
    registry: docker.io
    repository: getmeili/meilisearch
    tag: v1.47
  persistence:
    size: 5Gi
    storageClass: ceph-block-r1
  # Pod resources — sized per-env in the consuming overlays.
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi

## @section ExternalSecrets
externalSecrets:
  storeName: cluster-secret-store
  storeKind: ClusterSecretStore
  refreshInterval: 1h

  ## ASM keys.
  asmKeys:
    # ONE consolidated tenant bundle — N27a/<namespace>. ESO extracts it and
    # splits it into every app Secret (spree-rails, spree-db, cms-payload,
    # cms-db, spree-smtp, revalidate, resend, webhook). Env-neutral: one AWS
    # account per env post the Staging-account split, so the bundle carries flat
    # unsuffixed keys. Set in the gitops base overlay. See
    # infra/ASM-CONSOLIDATION-PLAN.md.
    bundle: ""
    # service_user/<acct>/* IAM-key secrets stay separate (env-scoped); the env
    # overlay supplies these.
    spreeS3: ""
    cmsS3: ""
    cnpgBackup: ""

## @section S3 buckets — env overlay supplies.
## Under `global` so they propagate to the rails-server / nextjs subcharts,
## where the tenant-base extraEnvVars reference them via `tpl`
## (AWS_BUCKET for Spree Active Storage, S3_BUCKET for Payload).
global:
  s3:
    spreeBucket: ""        # env overlay: tkf-spree-<env>-storage-...
    cmsBucket: ""          # env overlay: tkf-payload-<env>-media-...

## @section Image tags for the cms-migrate hook Job + hash-naming. Empty
## defaults so the bare chart renders/lints; the gitops env overlay supplies
## the real tags (kept in lockstep with web/cms.image.tag).
images:
  spree: ""
  storefront: ""
  cms: ""

## @section CMS migrate Job (Payload). Lives in the umbrella because the
## cms image is the nextjs subchart's payload-shaped consumer; rails-task
## is Rails-specific.
jobs:
  cmsMigrate:
    enabled: true
    backoffLimit: 3

  ## @section Nightly prod→staging data refresh (sanitize-before-load).
  ## Renders a CronJob ONLY when enabled (stg overlay). Rebuilds stg's spree +
  ## payload DBs from prod's barman backup via an in-pod throwaway Postgres, then
  ## sanitizes (backend `tkf:staging:sanitize`) before loading into stg's tkf-db.
  ## NEVER touches the prod database. See templates/cronjob-staging-refresh.yaml.
  ## Needs k8s >= 1.28 (native sidecars) — verify on the target cluster.
  stagingRefresh:
    enabled: false
    schedule: "0 3 * * *"          # 3h after prod's 00:00 backup settles
    concurrencyPolicy: Forbid       # never overlap a destructive run
    backoffLimit: 1                 # don't aggressively retry
    successfulJobsHistoryLimit: 3
    failedJobsHistoryLimit: 3
    ttlSecondsAfterFinished: 86400  # reap the pod (+ ephemeral PVC) after a day
    # CNPG operand image (barman-cloud-* + pg_dump/restore + postgres). MUST match
    # prod's PG major. Empty → defaults to .Values.cnpg.imageName at render.
    pgImage: ""
    awsCliImage: amazon/aws-cli:2.17.0
    awsRegion: eu-west-2
    # IAM-key Secret granting: read prod backup + prod buckets, write stg buckets.
    awsSecret: ""                   # e.g. tkf-stg-refresh (env overlay)
    # ASM source for awsSecret. When set (with enabled), the chart materialises
    # awsSecret (AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY) from this ASM key via
    # ESO — same pattern as asmKeys.spreeS3. e.g. service_user/prod/tkf-stg-refresh
    asmKey: ""
    # Ephemeral scratch holding $PGDATA + WAL + dumps (sized near prod data).
    scratchVolume:
      storageClass: ceph-block-r1
      size: 60Gi
    # Source: prod's existing barman backup in S3. serverName empty → cnpg.name.
    backup:
      bucket: ""                    # env overlay (prod backup bucket)
      path: ""                      # env overlay (e.g. pg/tkf/prd)
      serverName: ""
    # Databases to restore/migrate/dump/load. owner == role to trust-connect as
    # locally and the stg role to restore as; passwordSecret holds the stg role
    # password (key `password`). Defaults match the tkf base cnpg.databases.
    databases:
      - { name: spree, owner: spree, passwordSecret: "" }
      - { name: payload, owner: payload, passwordSecret: "" }
    # prod→stg media bucket pairs for the aws s3 sync phase (no PII).
    mediaSync: []
    #  - { from: tkf-spree-storage-...-<prdAcct>, to: tkf-spree-storage-...-<stgAcct> }
    # Extra env injected into the Rails migrate/sanitize phases (alongside the
    # template-set DATABASE_URL). Supply SMTP_FROM_ADDRESS (prod boot-guard),
    # REDIS_URL (Sidekiq clear target), STORE_URL, RAILS_HOST, TKF_ALLOW_SANITIZE,
    # STAGING_SPREE_PUBLISHABLE_KEY here from the env overlay.
    railsEnv: []
    resources: {}

## @section Subchart defaults — generic Rails / Next defaults that work
## across tenants. Tenant base overlay supplies image registry + repository;
## env overlay supplies the tag and replica count.
##
## Aliases:
##   web, worker         -> rails-server (Deployments)
##   migrate, seed, rake -> rails-task   (Jobs, helm post-install hooks)
##   storefront, cms     -> nextjs       (Deployments)

_railsSecurity: &railsSecurity
  podSecurityContext:
    enabled: true
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000
    fsGroupChangePolicy: Always
  containerSecurityContext:
    enabled: true
    runAsUser: 1000
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: [ALL]
    seccompProfile:
      type: RuntimeDefault

_nextSecurity: &nextSecurity
  podSecurityContext:
    enabled: true
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000
  containerSecurityContext:
    enabled: true
    runAsUser: 1000
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: [ALL]
    seccompProfile:
      type: RuntimeDefault

web:
  <<: *railsSecurity
  command: [bundle, exec, rails, server, "-b", "0.0.0.0", "-p", "3000"]
  service:
    enabled: true
    ports:
      http: 80
  containerPorts:
    http: 3000
  resourcesPreset: none
  # Graceful rollout (lever B): a preStop drain lets the gateway deregister this
  # endpoint before SIGTERM, so an umbrella roll doesn't sever in-flight storefront
  # SSR fetches mid-request (the "Connection closed" hangs). Never drop below
  # desired replicas during the roll.
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  terminationGracePeriodSeconds: 40
  lifecycleHooks:
    preStop:
      exec:
        command: ["sh", "-c", "sleep 10"]

worker:
  <<: *railsSecurity
  command: [bundle, exec, sidekiq]
  service:
    enabled: false
  livenessProbe:
    enabled: false
  readinessProbe:
    enabled: false
  resourcesPreset: none

migrate:
  <<: *railsSecurity
  command: [bundle, exec, rails, db:prepare]
  hook: post-install,post-upgrade
  hookWeight: "-5"
  hookDeletePolicy: before-hook-creation
  backoffLimit: 3
  resourcesPreset: none

seed:
  <<: *railsSecurity
  command: [bundle, exec, rails, db:seed]
  hook: post-install
  hookWeight: "0"
  hookDeletePolicy: before-hook-creation
  backoffLimit: 1
  resourcesPreset: none

rake:
  <<: *railsSecurity
  command: [bundle, exec, rake, --version]  # tenant overrides per env
  hook: post-install,post-upgrade
  hookWeight: "5"
  hookDeletePolicy: before-hook-creation
  backoffLimit: 1
  resourcesPreset: none

storefront:
  <<: *nextSecurity
  service:
    enabled: true
    ports:
      http: 80
  containerPorts:
    http: 3001
  resourcesPreset: none
  # The storefront runs Next.js standalone with cacheComponents on a read-only
  # root. Seed a writable .next/server so ISR / Cache Components can persist
  # their render cache instead of failing with EROFS on every revalidation.
  writableNextServer:
    enabled: true
  # Graceful rollout (lever B): drain before SIGTERM so a roll doesn't drop the
  # in-flight HTML document, and never drop below desired replicas.
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  terminationGracePeriodSeconds: 40
  lifecycleHooks:
    preStop:
      exec:
        command: ["sh", "-c", "sleep 10"]

cms:
  <<: *nextSecurity
  service:
    enabled: true
    ports:
      http: 80
  containerPorts:
    http: 3000
  resourcesPreset: none
